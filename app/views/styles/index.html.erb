<div class="pg">
<div class="styles_wrapper">
  
       
      
       <% @genres.each do |genre| %>
         <% unless genre.name == 'No genre' %>
           <%=link_to genre_path(genre) do %> 
           <div class="style_div" id="<%= genre.name%>">
            <!-- <h3>
              %= genre.name % <br/>
            </h3> -->
           </div>  
          <% end %>
              
         <% end %>
          
       <% end %>
      </ul>

  </div>
  </div>

  <script>
  $('.side_nav').css('display','none');
  $('body').css('background-color','white');
  </script>